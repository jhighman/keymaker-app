{
  "name": "keymaker-backend",
  "version": "1.0.0",
  "description": "Backend API for KeyMaker application",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "scheduler": "node scheduler.js",
    "scheduler:cron": "node scheduler.js && echo 'Scheduler completed'",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "mongoose": "^7.0.3"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^0.34.6",
    "mongodb-memory-server": "^10.1.4",
    "nodemon": "^2.0.22",
    "supertest": "^6.3.4",
    "vitest": "^0.34.6"
  }
}
